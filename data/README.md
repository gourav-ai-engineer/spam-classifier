# Dataset

Place a CSV dataset here. The loader accepts common column names such as `label`/`text` or the SMS Spam Collection `v1`/`v2` format.

Labels may be `ham`/`spam` or `0`/`1`.

Example command:

```bash
python train.py --data data/spam.csv
```
